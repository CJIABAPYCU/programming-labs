package model;

/**
 * Представляет сущность, которая может проверять свои ограничения.
 */
public interface Validatable {
    /**
     * Проверяет, что текущий объект удовлетворяет всем ограничениям.
     *
     * @return true, если объект валиден
     */
    boolean validate();
}
